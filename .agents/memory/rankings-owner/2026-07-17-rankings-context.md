---
title: "Rankings system context (living doc)"
date_created: "2026-07-13"
date_updated: "2026-07-23"
description: "Core knowledge of the rankings chart: architecture, mechanics, current work state."
when_to_use: "Read at session start before touching PointsBarChart or rankings data."
---

# Rankings context

## 2026-07-23 — LIVE POINTS COLUMNS: UI v6 (inline `current / def` slash; DEF decoupled from playing; side-by-side hover)
Boss iteration on v5. Cell is now ONE inline row: `+liveEarned`(green) `/`(muted) `defendingPoints`(muted), same level, slash-split. Dropped the DEF chip.
- **Placeholders:** each side is its number OR a long-dash `—` when absent → `+25 / —` (playing, nothing to defend), `— / 25` (defending, not re-entered), `+50 / 100` (both). NEITHER live nor defending → the single big `—` (unchanged empty state). Breathing green dot still precedes current iff `lp.liveIn` (player still in event).
- **DEFENDING DECOUPLED FROM PLAYING (new capability):** a player defends last-year points at an ongoing event even if NOT entered this year (they bleed those points live). Added `ongoingEventNames(players)` → Set of all live event names in the field; `findDefending(vis, live, ongoingNames?)` 3rd arg finds a non-live visible result whose name ∈ ongoing (prefers the event the player is themselves in). `computeLive(player, now, ongoingNames?)` + `liveRanks` computes the set once. Pages pass `ongoingNames={ongoing}` to `<PlayerRow>`. **Chart's 2-arg `findDefending(vis, live)` UNCHANGED** (its live pane only wants the player's own event). Home single-row demo omits the prop → defending only shows when the player plays (fine).
- **0-point defending filtered:** `findDefending` requires `points_earned > 0` (a prior 1st-round loss defends nothing) → kills `— / 0` / `+25 / 0` noise. 0 doesn't affect livePoints math anyway.
- **Math now MORE correct:** a not-playing defender gets `defendingPoints>0, liveEarned=0 → delta=−def`, livePoints drops → live rank reflects it. Verified: player defending a title (Croatia W 250) not re-entered while playing Nordea R16 25 → `+25 / 250`, hover `THIS WEEK −225 pts ↓5`.
- **Hover card: two tournament cards SIDE BY SIDE** (was stacked). Summary row on top (full width), then `flex gap-3` with LIVE | DEFENDING, `border-l` divider between; each `min-w-40 flex-1`. Single card when only one side. Removed outer `min-w-48`.
- **Verified 2026-07-23:** `pnpm check` 0/0, no h-overflow 1440. Real data already exercises decoupling (`— / 25` renders for non-playing Croatia/Swiss/Nordea defenders — verified via scratchpad `hover.mjs list` mode dumping all slash-cell texts). Dots verified via temp `out` patch (27/40 out:false → probe.mjs counted 27 `.animate-ping` in DOM, screenshot showed `● +25 / —`); data restored git-clean. Two-card + single-card hover both screenshotted.
- **CACHE GOTCHA (cost a debug loop):** `getRankings` (`tennis-api.ts`) caches the snapshot IN-MEMORY server-side (`CACHE_TTL_MS`). Patching `static/data/{tour}.json` while `pnpm dev` runs does NOT take effect — the page serves the cached copy (curl of `/data/atp.json` shows the patch, but the PAGE loader uses the cache). **Must RESTART dev after editing the json** to bust the cache.
- **Open:** (1) `+0 / …` shows for a player in-event with 0 banked pts (honest floor, has dot) — fine. (2) still: cell dot GREEN vs chart-pane dot RED. (3) dots still dark on real committed data (no `out` until refresh). (4) a player defending at 2 ongoing events shows only ONE (first by date) — rare, single-slot UI.

## 2026-07-23 — LIVE POINTS COLUMNS: UI v5 (single current/def layout; dot = player-IN — FIRST ETL/data change)
Boss killed the two-mode split (v4). Now ONE layout for every live entry: `+liveEarned` (green) over `DEF` chip + `defendingPoints` (grey). The ONLY variable = the breathing green dot, and boss redefined its meaning: **dot = player still IN the tournament, NOT event-over.** This finally required the per-player elimination data every prior live iteration avoided — **first ETL + data-format change of the live feature.**
- **ETL (`etl.ts condenseTournament`):** derive alive from the raw draw — a player is alive at their deepest match iff `m.match_winner == null` (in play) OR `=== pid` (won, advanced); lost it → eliminated. Champion → alive. Stored on archive `players[pid].alive` (new required field, `types.ts TournamentArchive`). `buildDistribution` maps ongoing → `{ live:true, out: !entry.alive }`.
- **Type (`types.ts`):** `TournamentResult.out?: boolean` — live entry only; true = eliminated (earned now final), absent/false = still in.
- **live-points.ts:** replaced v4's `liveActive` (event-end time check, now DELETED) with `liveIn: live.out === false` (EXPLICIT false only — absent flag on pre-refresh data reads as NOT-in, so no misleading dot before a refresh populates `out`).
- **UI (`PlayerRow.svelte`):** `{#if lp.live}` → row of [dot if `lp.liveIn`] + `+earned` green, then `{#if lp.defending}` DEF chip + defpts. No live entry → `—`. v4's settled-delta branch gone. Header still **THIS WEEK**. Hover card unchanged (v3 combined card).
- **DATA CAVEAT (told boss, important):** committed `{tour}.json` has NO `out` field → `liveIn` false everywhere → **NO dots until a `refresh:data` runs during a live tournament**. Also WTA still on scratchpad patch (never sets `out`) → WTA never dots until it goes native. Offline-rebuild from old archives: `entry.alive` undefined → `out:!undefined`=true → no dot (safe).
- **Verified 2026-07-23:** `pnpm check` 0/0, no h-overflow 1440. Dots can't show on real data (no `out`), so verified via a TEMP patch (scratchpad node one-liner set `out` alternating on the 40 atp live entries, backup→patch→shot→restore, json git-clean after): confirmed `● +25` (in), `+25` no-dot (out), `● +50`+`DEF 100` grey (in+defending) all render correctly; `+earned` always green.
- **Open:** (1) cell dot GREEN, chart-pane live dot RED — still inconsistent (boss aware, may reconcile). (2) elimination accuracy bounded by archive `end_date`/draw lag — a just-lost player may show dot until next refresh re-condenses the draw. (3) feature dark until next live-week refresh.

## 2026-07-23 — LIVE POINTS COLUMNS: UI v4 (THIS WEEK modes — supersedes v3 cell)
Boss re-strategized the merged col same session (v3 cell layout lived ~1 iteration). Header now **THIS WEEK** (was "Live"). Live-points TOTAL removed from row entirely (only official Points remains as a number col). Cell = movement only, two modes:
- **Mode 1 (active — live event still running):** breathing green dot (Tailwind ping ring + solid core, `size-2 bg-green-500`) + `+liveEarned` green; second row `DEF` chip (rounded `bg-muted` `text-[9px]`) + `defendingPoints` grey, NO minus sign (chip labels it). Boss spec: "<breathing dot> + current (green) / <chip DEF> <def pts> (grey)".
- **Mode 2 (settled — event ended, not yet folded into official pts):** single `signed(delta)` `text-sm` bold green/red. Boss: "player out or won tournament".
- **`liveActive` added to `LivePoints`** (`live-points.ts` computeLive): `now < localDate(event_date_end,'T24:00:00')` (T24 = next-midnight, valid ISO, covers full final day). NEEDED because settled-mode was otherwise DEAD CODE: `findDefending` requires `live`, so `delta≠0` impossible without a live entry — the only real "settled" state is a live-flagged event whose end date passed (staleness window until next refresh). Mode gate: `lp.live && lp.liveActive` → mode 1; `else if delta≠0` → mode 2; else dash.
- **Data limitations (told boss):** (1) no per-player elimination — knocked-out player stays mode 1 (dot) until EVENT ends; (2) ongoing-archive `end_date` lags to last-played-match date → may flip to settled a day or two early while finals unfetched.
- Hover card (v3 combined card) UNCHANGED — still whole-cell trigger, THIS WEEK summary + LIVE + DEFENDING sections.
- **Verified 2026-07-23:** `pnpm check` 0/0, no h-overflow 1440. Real clock (Jul 23, all archived events ended Jul 16) → ALL rows mode 2 (`+25` green / `−50` red / dash) — correct, no dots. SIM_NOW=`2026-07-15T15:00:00` → mode 1 renders (`● +25`+`DEF 25`, `● +50`+`DEF 100`); SIM_NOW reverted to null after shots. hover.mjs selector updated (`[aria-label*="this week"]`).
- **Open:** dot color green chosen (matches earned number); chart pane dot is RED — inconsistency boss may want reconciled. Data 6 days stale — a `refresh:data` would clear the ended live flags and empty the col until next tournament week.

## 2026-07-23 — LIVE POINTS COLUMNS: UI v3 (merged Live + This Week → ONE col)
Boss merged the **Live** and **This Week** columns into a single "Live" column. Layout now 5 desktop cols: **Rank | Player | Points | Live | Distribution** (was 6).
- **Merged Live cell** (`PlayerRow.svelte`): TOP = `livePoints` (`text-sm` bold, always shown). BOTTOM = `+liveEarned` (primary/green) / `−defendingPoints` (muted), only when `lp.live || lp.defending`. Non-playing player (no activity) = just the top number, no card.
- **Net swing + rank arrow MOVED OFF-ROW into the hover card** (boss: "move to hover card"). They no longer render inline — cleaner cell.
- **Single hover card for the whole cell** (was two separate span-triggers). `cardAnchor` state (Element|null) + `hasActivity` derived (`delta!==0 || rankDelta || live || defending`). Only the activity branch gets the hover wrapper (role-less, `cursor-help`, `onmouseenter`/`leave`; still no interactive nesting inside row's `role=button`). Card body reads this row's derived `lp` directly (per-row component instance). Sections, each optional, `border-t` divided: (1) **THIS WEEK** summary = `signed(delta) pts` (colored by delta sign) + rank `ArrowUp/Down`+|rankDelta| (colored by rankDelta sign, INDEPENDENT); (2) **LIVE** badge + `TournamentCard`; (3) **DEFENDING** badge + `TournamentCard`. Removed old `card`/`showCard`/`{result,badge,ptsClass}` single-card machinery + unused `TournamentResult` import.
- **Grid v3** (both files): live-on template `grid-cols-[3rem_1.25fr_4.5rem_6rem_minmax(0,1.75fr)]` (was 6-col `..._4.5rem_4.5rem_7rem_...`). Merged col = `6rem`. `RankingsHeader.svelte` mirrors: headers **Points / Live** (dropped "This Week"). OFF branch untouched.
- **Verified 2026-07-23:** `pnpm check` 0/0. CDP shots (scratchpad `shot.mjs` + new `hover.mjs`): 1440 desktop no h-overflow (sw==cw==1440); merged col reads Cobolli(9) `3,485`/`+25`, non-playing top-8 just the number. Hover card confirmed both shapes — live-only (Ruud: "THIS WEEK +25 pts" + LIVE EFG Swiss Open R16 25pts) AND two-card+arrow (a player: "THIS WEEK −50 pts ↓8" red / LIVE Croatia Open QF 50pts green / DEFENDING same event SF 100pts). Independent net/rank coloring holds (net red, earned green, def gray on same card). `hover.mjs` note: page scroller is NOT window (window.scrollTo no-op, sy stays 0) — screenshot deep rows via a TALL device height (13000) + captureBeyondViewport, not scroll.
- **Open / boss may nudge:** (1) still TWO "Live" labels (merged col header + chart-pane "Live" far right) — merge shrank the redundancy question but didn't kill it. (2) `6rem` merged-col width is a guess; `+50 −100` fits, dial if wider values clip. (3) net-swing number now ONLY in the card (was inline) — if boss wants an at-a-glance swing without hovering, add back a compact inline hint.

## 2026-07-18 — LIVE POINTS COLUMNS: UI v2 (col regroup)
Boss iteration on the live block. New 3-col layout (was Total | Live | Result):
- **POINTS** (`current_points`): bumped `text-sm`→`text-lg` bold = most prominent number. Boss wanted it "more prominent + slid left".
- **LIVE** (`livePoints`): single number, `text-sm` bold. Net-delta + rank-arrow REMOVED from here (moved to next col).
- **THIS WEEK** (the old Result col, now 2×2): TOP row = `signed(delta)` (net) + rank move `ArrowUp/Down`+|rankDelta|; BOTTOM row = `+liveEarned` (hover Live card) + `−defendingPoints` (hover Defending card). Empty-cell (non-playing, no move) collapses to a SINGLE `—` (was two stacked dashes).
- **Coloring confirmed INDEPENDENT** (boss Q: player can gain rank while losing pts): net colored by `lp.delta` sign, rank arrow by `rankDelta` sign — two separate spans, own conditions. Verified in data: row 2,435→2,410 shows red `−25` net / green `+25` earned / gray `−50` def; row 2,210→1,985 shows `−225 ↓5` (both red) / `+25 −250`. A mixed green-net+red-arrow case renders correctly by construction (none in current data to screenshot).
- **Grid v2** (both files, JIT): live-on md template `grid-cols-[3rem_1.25fr_4.5rem_4.5rem_7rem_minmax(0,1.75fr)]` (was `[3rem_1.5fr_3.5rem_5rem_5rem_minmax(0,1.5fr)]`). Player fr 1.5→1.25 + dist 1.5→1.75 = shifts fixed block LEFT ("slide points left"). `RankingsHeader.svelte` mirrors: headers Points / Live / This Week (was Total/Live/Result). OFF branch untouched.
- Verified 2026-07-18: `pnpm check` clean, CDP shot 1440 no h-overflow (sw==cw==1440), single-dash collapse confirmed on non-playing top-8.
- **Open / boss may nudge:** (1) `text-lg` POINTS maybe too big — easy dial-back. (2) how far "left" the points slid is a judgment nudge (fr ratio) — can push further. (3) `text-primary` earned reads green ≈ same hue as green net on positive rows (distinct on negative-net rows where net is red) — acceptable, flag if confusing.

## 2026-07-17 — LIVE POINTS COLUMNS: IMPLEMENTED (v1)
Shipped the 3-column live-points block on ranking rows (`.idea/LIVE-POINTS.md` spec). Pure frontend — ZERO API/ETL/data-format change (all derived from `points_distribution` already in snapshot).
- **New `src/lib/live-points.ts`** — the single source of live math, shared by chart + rows so they can't drift. Exports `localDate`, `mondayOnOrAfter`, `isVisible`, `visibleResults`, `findLive`, `findDefending`, `computeLive(player)→{live,defending,liveEarned,defendingPoints,livePoints,delta}`, `liveRanks(players)→Map<name,liveRank>`. Formula: `livePoints = current_points − defendingPoints + liveEarned`, `delta = liveEarned − defendingPoints`. All default `now = getNow()`.
- **`PointsBarChart.svelte` REFACTORED** to consume the util: `visibleResults=computeVisible(results,today)`, `liveResult=showLivePoints?findLive(...):null`, `defendingResult=findDefending(...)`. Deleted its local `localDate`/`mondayOnOrAfter`/inline-expiry-filter (now imported/util). `localDate` still imported for xPos/axisX. Behavior unchanged (verified via screenshot: bars, live pane ●, GS logos, year axis intact).
- **New `src/lib/components/TournamentCard.svelte`** — presentational card body (event name/type/dates/result+pts). Extracted from the chart's old `{#snippet tournamentCard}` + `fmtMD`. Used by BOTH chart tooltip and row hover cards.
- **`PlayerRow.svelte` REWRITTEN** — new props `rankDelta?`. `lp=$derived(computeLive(player))`. When `SHOW_LIVE_POINTS`: 3 desktop cols (`hidden md:flex flex-col items-center`): **Total** (`current_points`, no "pts"), **Live** (top livePoints; bottom `signed(delta)` green/red + rank `ArrowUp/Down`+|rankDelta| green/red; `—` if both 0), **Result** (top `+liveEarned` hover→Live card / `—`; bottom `−defendingPoints` hover→Defending card / blank). Hover cards = Floating UI (`computePosition`+offset/flip/shift+autoUpdate) mirroring chart tooltip; `card` state, `showCard()`. Inner spans `role=button tabindex=0` + `onclick`/`onkeydown` **stopPropagation** so they don't toggle the row's select. Flag OFF → renders ORIGINAL single right-aligned Points col (revert-clean).
- **Grid sync = 2 files, flag-branched.** LIVE-ON md template: `grid-cols-[3rem_1.5fr_3.5rem_5rem_5rem_minmax(0,1.5fr)]`; OFF = original `[3rem_1.5fr_6rem_minmax(0,1.5fr)]`. Both literals present in source (ternary) so Tailwind JIT emits both. `RankingsHeader.svelte` mirrors both branches (headers Total/Live/Result centered + Distribution).
- **Rank delta wired in atp + wta `+page.svelte`**: `const rankMap=$derived(liveRanks(players))`; `rankDelta={player.rank - (rankMap.get(player.name) ?? player.rank)}`. `>0`=moved up=green ArrowUp. HOME page omits rankDelta (single-row demo, no full field) — optional prop renders clean.
- **Verified 2026-07-17:** `pnpm check` 0 errors/0 warnings. CDP screenshots (scratchpad `shot.mjs`, node22 global WebSocket = browser API `addEventListener` NOT `.on`; chrome `C:/Program Files/Google/Chrome/Application/chrome.exe`): desktop 1440 no h-overflow (sw==cw), cols aligned, Cobolli(9) live = Live `3,485 +25` green / Result `+25`; non-playing top players Total==Live + `—`. Mobile 414 (emulated mobile:true) unchanged — live cols hidden, no overflow.
- **A11y fix (post adversarial review, 2026-07-17):** hover-trigger spans were `role="button" tabindex=0` nested INSIDE the row's own `role=button` → axe nested-interactive violation + dead buttons (Enter/Space did nothing) + Space-scrolled page (no preventDefault). Fix: stripped ALL button/keyboard semantics — spans are now role-less hover-only (`onmouseenter`/`onmouseleave` + `aria-label` + `cursor-help`, `<!-- svelte-ignore a11y_no_static_element_interactions -->`). Clicking the number now just toggles the row like any cell (consistent). Trade-off: NO keyboard reveal of the hover card (desktop hover enhancement; aria-label still names the tournament; same limitation as the chart's hit-areas). Full WAI-ARIA tooltip pattern (focusable trigger + role=tooltip + Esc) deferred. `pnpm check` still clean.
- **Review workflow:** 5-dimension adversarial (live-math / refactor-equiv / svelte-reactivity / layout-a11y / rank-delta), each finding refuted by a skeptic. 3 confirmed (all the a11y nesting above, 1 medium 2 low), 0 in math/refactor/rank-delta — live formula, chart-refactor equivalence, and rank-delta sign all verified clean.
- **Polish notes / open (v1 accepted):** (1) non-playing player shows Live==Total (redundant but honest) — could dim when identical. (2) TWO "LIVE" headers (the Live COLUMN + chart-pane "Live" far right) — mild redundancy, maybe rename one. (3) top rows look sparse mid-July (top guys skip 250s → mostly dashes); feature lights up for lower ranks currently playing. Deferred: sort-by-live-points, mobile display, official `wk`/`wkPts` capture, keyboard-accessible hover cards.

## 2026-07-17 — LIVE POINTS COLUMNS: API investigation (source-of-truth question)
Boss approved ≤10 calls to probe API for a live-ranking source. Spent 4 (REST, real `RAPIDAPI_KEY` from `.env` — MCP gateway key is INVALID/rate-limited, "Invalid API key"/"Too many requests"; use REST host `tennis-api-atp-wta-itf.p.rapidapi.com` directly).
- **NO official "live ranking" endpoint.** Ranking `group` options = `singles,doubles,race,surface,prize` (from `Full_Rankings` schema). `race` = calendar-YTD points (Sinner 7950 vs 52wk 13450), NOT a live/if-tournament-ended ranking. So NO API source of truth for live points — every site derives them. **Decision: DERIVE** `live = current_points − defending + live_earned`, re-rank loaded field for the rank-move arrow. Zero new calls, zero data-format change for v1 (all derivable from `points_distribution` already in `{tour}.json`).
- **Singles ranking row carries FREE extra fields we currently discard** (`fetchRanking` only reads `pts`): each row = `{date, pts, wk, yr, wkPts, position, player:{id,name,countryAcr}}`. `wk` = official rank move vs last week (Zverev +1, Alcaraz −1), `yr` = rank move vs year start, `wkPts` = official points delta this week (Zverev +1290, Alcaraz −1300 = Wimbledon settling). This is OFFICIAL PAST-WEEK movement (last Monday vs prior), DISTINCT from live/in-progress movement. Capturable for zero extra calls (just read more fields in etl.ts). **Boss chose LIVE-DERIVED arrow** (official_rank − live_rank via re-ranking loaded top-100 on live_points); wk/wkPts NOT used in v1 (parked, still free later). Limitation: rank delta only sees loaded top-100.
- `ranking/{tour}/filters` = just a country list (for country filtering), NOT ranking-type discovery.
- **Live-events feed exists** (`GET /tennis/v2/extend/api/events/live`): in-progress matches, ALL levels (sample = only ITF M15/W15, no tour-level at probe time), fields `{id,name,participant1/2,league,score,status:"InPlay",points,indicator,tourType,startTimestamp,matchId}`. `matchId` = `p1Id-p2Id-tourId-roundId`. NO points/tournament-points context. Only relevant for a future TRUE real-time upgrade (boss chose as-of-refresh) — big lift (map matches→tour events→round points). Parked.
- **Boss decisions (2026-07-17):** freshness = as-of-last-refresh (keep static arch); sorting-by-live-points = LATER (not v1); mobile = LEAVE OUT (desktop-only, matches current Points col). Source = derive (pending final confirm + wk/wkPts bonus Q).
- **Grid sync points = exactly 2 files:** `RankingsHeader.svelte` + `PlayerRow.svelte` (its `md:` string). Pages (atp/wta) + home all render shared `<RankingsHeader />` + `<PlayerRow>` — no per-page grid string. Home `+page.ts` loads FULL atp.json+wta.json (renders 1 row) so full field available for rank re-rank there too.

## 2026-07-17 — LIVE FEED LANDED (real ongoing-tournament data)
Live bar no longer placeholder. Now driven by real in-progress tournament data. Key change: **detection is by a `live` flag, NOT anniversary date-projection anymore.**
- **Data path (zero new API calls needed):** ETL already archives ongoing tournaments as `{status:'ongoing', players:{id→deepest round SO FAR}}` and pivots them by playerId into each top-100 player's `points_distribution`. So the in-progress event is ALREADY in `static/data/{tour}.json` after a refresh — it was just indistinguishable from a finished bar.
- **The flag:** added `live?: boolean` to `TournamentResult` (`types.ts`). `etl.ts buildDistribution` sets `live:true` when the source archive `status==='ongoing'`. `points_earned`/`result` = round reached so far (a FLOOR, e.g. R16 of a 250 = 25pts; R32 of a 250 = 0pts → tiny 2px bar, honest).
- **Chart (`PointsBarChart.svelte`):** DELETED `toThisYear`/`isCurrent`/`currentResult`/`LIVE_PLACEHOLDER`/`liveDisplay`/`currentTip`/`pos_*`. New: `liveResult = visibleResults.find(r=>r.live)`; `defendingResult` = same `event_name`, non-live (last yr's edition, matched by name); `mainResults = visibleResults.filter(r=>!r.live)` feeds all main-chart loops so the live entry does NOT double-render as a normal bar. Live pane uses real `liveResult.result`/`points_earned`. Tooltip (`liveTip`) two-pane Live|Defending; **Defending pane is now optional** (`{#if tooltip.defending}` — many small-250 live players weren't in last yr's edition → single-pane Live card, min-w-48).
- **Flag flipped ON:** `SHOW_LIVE_POINTS = true` (`flags.ts`). `SIM_NOW = null` now (real clock).
- **id→name map added** (`etl.ts writePlayersMap` → `static/data/players/{tour}.json`, merged/grown each run). FREE — sourced from the ranking response already fetched. Covers RANKED players only (top ~100); draw-only opponents NOT captured (would need draw responses, which we condense away). Purpose: make id-keyed archives human-readable + future features. **Only populates on next `refresh:data` run** (forward-looking).
- **atp.json now built NATIVELY** (2026-07-17): spent 1 real ATP ranking call → seeded `static/data/rankings/atp/2026-07-13.json` (+ `players/atp.json`, 100 ids) → real `--offline` rebuild wrote atp.json with 40 live players flagged by the ETL itself (no patch). **WTA still on the scratchpad patch** (`patch-live.mjs`, 23 players) — not seeded (no wta ranking archive yet); a future online `refresh:data` OR 1 wta ranking call + offline rebuild would make it native too. wta `--offline` currently THROWS (no seeded ranking).
- **One-off patch (superseded for atp, still live for wta):** scratchpad `patch-live.mjs` reads local ongoing archives, sets `live:true` on matching snapshot entries (key `event_name|event_date_start`), rewrites `{tour}.json`. 0 API calls.
- **Raw ranking now archived** (`etl.ts loadOrFetchRanking` → `static/data/rankings/{tour}/{YYYY-MM-DD}.json`, keyed by publication Monday; `{tour, ranking_date, fetched_at, data:[raw rows]}`). Reused within the same week = **0 ranking calls on same-week re-runs**; `--force` re-fetches. It's the ONLY id↔name↔rank↔pts join (snapshot drops IDs), so archives + ranking archive = enough to rebuild `{tour}.json` fully offline. Also a weekly ranking history for free. **Seeds on next `refresh:data`** (empty until then — the offline-rebuild property needs ≥1 archived ranking to exist).
- **`--offline` mode added** (`refresh:data -- --offline`): rebuilds `{tour}.json` from local archives + newest archived ranking, ZERO API calls. `refreshTour` early-returns for offline; shared `finishSnapshot(tour, inWindow, ranking)` does the pivot for both paths. Helpers `readAllArchives` (all archive files for a tour) + `loadLatestRanking` (newest `rankings/{tour}/*.json`, throws if none seeded). Verified 2026-07-17 via throwaway harness w/ fabricated ranking (real IDs 13447/34539): read 63 archived tournaments, built snapshot, live flags propagated (Croatia Open QF 50pts). NEEDS ≥1 seeded ranking archive first (empty until an online run).
- **Expiry logic UNTOUCHED** by the live rework (boss asked 2026-07-17). Still `mondayOnOrAfter(event_date_end)` +364d, floored to midnight, visible while `today < expiry`. Deleted code was anniversary x-positioning only. Verified live: Wimbledon 2025 (award Mon 2025-07-14 → expiry 2026-07-13) correctly absent at today 2026-07-17.
- **Verified 2026-07-17:** `pnpm check` clean. Headless-Chrome screenshots: Cobolli (rank 9) live pane shows `● 16`, Ruud (13) hover popover = LIVE EFG Swiss Open R16 25pts | DEFENDING QF 50pts (2025). Blank pane for non-playing top players (Sinner etc.) — correct.
- **Open:** live bars for R32-of-250 show 0pts (2px stub) — honest but visually weak; boss may want "projected if wins" later. Ongoing archive `end_date` lags to last-played-match date (finals not yet fetched) — fine. Next real `refresh:data` (~10-14 calls) refreshes ongoing depth + writes flag + players.json natively.

## Memory convention
Files named `YYYY-MM-DD-topic.md` (date = last updated), YAML header. This file is the living doc; spin off dated decision files when it gets big.

## Architecture (see AGENTS.md for full detail)
- SvelteKit 2 + Svelte 5 runes + Tailwind 4. Offline ETL writes `static/data/{tour}.json`; runtime only reads static JSON. Pages prerendered.
- Main viz: `src/lib/components/PointsBarChart.svelte`, used by `PlayerRow.svelte` (grid `grid-cols-[3rem_1.5fr_6rem_minmax(0,1.5fr)]`, keep in sync with RankingsHeader + page headers).
- Types in `src/lib/types.ts` (`TournamentResult`, `Player`).
- Data as of 2026-06-28 refresh: contains Wimbledon 2025 (ends 2025-07-13) but NOT Wimbledon 2026.

## Chart mechanics (as of 2026-07-13 session, pass 5)
- FEATURE FLAG (pass 5, boss request): `src/lib/flags.ts` exports `SHOW_LIVE_POINTS` (default FALSE = live work hidden for now). PointsBarChart takes prop `showLivePoints = SHOW_LIVE_POINTS` (module flag is the default; prop override path = plausible future per-user setting). Single flip point. In component: `LIVE_W`/`LIVE_GAP` became `$derived` (26/10 when on, 0 when off) so `mainW == containerW` when off; live render gate is `live = $derived(showLivePoints ? currentResult : null)` used everywhere (content block, hit area, playerPeak autoscale); divider+pane-baseline gated on `{#if showLivePoints}`. OFF = clean full-width 52-week chart, no pane/divider/label/autoscale-bump; year-axis last-seg `x1 = containerW` naturally == mainW when off (doesn't extend past main chart). Both states verified via dev server screenshots (flip flags.ts true/false, HMR). `pnpm check` clean.
- Rolling window: main chart spans EXACTLY `windowStart = now - 364d` → `now` (no more +21d future zone / dashed today line — both removed). Bars at RAW `event_date_start`, clamped into `[0, mainW - BAR_W]`.
- LIVE PANE (only when flag ON; replaced today-line/future-zone design): separated section at right edge. `LIVE_W=26`, `LIVE_GAP=10`, divider line at `mainW + GAP/2`, own baseline stub. Width reserved when flag on (all rows) so time axes align across rows; content only when `live` (flag AND currentResult): pane tint, "● SF" inline dot+label, tier ghost, faded bar (opacity 0.4), GS logo. `mainW = containerW - LIVE_W - LIVE_GAP` ($derived); `liveCX = containerW - LIVE_W/2`. Y-max label at `mainW - 2`.
- LIVE indicator (pass 4, boss request — SUPERSEDES pass-3 "● LIVE"-above-bar): pulsing red dot now sits INLINE with the live round label = "● SF". Dot at `cx=liveCX-5, cy=ROUND_Y-3`; round text nudged to `x=liveCX+3.5` (still text-anchor middle). Separate "LIVE" text above the bar REMOVED. Dot still = 2 circles `live-dot-core` (opacity breathe) + `live-dot-ring` (scale ping), CSS keyframes unchanged, `transform-box: fill-box`. Ping ring max radius ~6.5 stays inside pane.
- Empty pane (player not in tournament, ~26/100 mid-Wimbledon): divider + baseline stub only. DECISION (boss man 2026-07-13): stays blank — blank = not playing. Keep alignment.
- YEAR AXIS (pass 3): per-segment span lines with `{#each yearSegments as seg, i}`: divider-side ends run flush INTO the Jan-1 vertical (no ±2 inset, end ticks removed — clean tee); outer ends (first seg left @ x0, last seg right) get chevron arrowheads (polyline, fill none) implying timeline continues; LAST segment's x1 = `containerW` so the latest year spans across the live pane. Guard `mid - 16 > x0 + 6` skips lines+arrows on cramped segments.
- AXIS PROMINENCE BUMP (pass 6, boss request): Jan-1 vertical divider stroke-width 1→1.5, stroke-opacity 0.65→0.85. Year span lines + arrowhead polylines stroke-width 0.8→1.25; arrowheads gained `stroke-linejoin/linecap=round` + chevron arm 4→5px for cleaner tips. All still `class="text-border"` (kept tasteful, bars still dominate). Verified both flag states via screenshots.
- Expiry = real ATP rule: `mondayOnOrAfter(event_date_end) + 364d`, visible while `now < expiry`. Verified correct (Wimbledon 2025 awarded Mon 2025-07-14, drops Mon 2026-07-13).
- Unexpired events can predate windowStart by up to ~2 weeks -> bar clamps to x=0 at far left. GS logo centers on bar with no clamp (svg overflow-visible, ~11px overhang OK).
- `toThisYear()` projection (pos_start/pos_end) used ONLY to detect the "current" tournament: anniversary window straddling `now`.
- TIMEZONE GOTCHA: date-only strings parse as UTC midnight but getDay/getMonth read local -> off-by-one in negative-offset tz. Always parse via `localDate()` (`dateStr + 'T12:00:00'`). Expiry floored to midnight so points drop at START of expiry Monday.
- Zoom-on-hover: Tween maxPts 2000 <-> playerPeak; `playerPeak` INCLUDES live placeholder points (autoscale covers live bar). Tier ghosts fade via tierOpacity.
- TOOLTIP: positioned by `@floating-ui/dom` (now a direct devDep; was already transitive via bits-ui). `computePosition` strategy:fixed, placement:top, middleware offset(10)/flip/shift(padding 8), wrapped in `autoUpdate`, anchored to the hovered hit-rect (`e.currentTarget`), no more mousemove tracking. Hidden (`visibility`) until first position resolves. Single outer div, kind branches inside. Two-pane (Live|Defending) card on live bar; single card elsewhere.
- Verified 2026-07-13 via headless Chrome + CDP scripts (scratchpad tooltip-test.mjs / scale-test.mjs pattern — Node 22 global WebSocket, Input.dispatchMouseEvent hover, screenshots): 74/100 ATP rows get live pane content mid-Wimbledon; right-edge two-pane card shifts inside viewport (would've overflowed 1490 > 1422 before); flips below bar when row at viewport top; De Minaur (peak 500) zooms y-axis to 800 = live placeholder. `pnpm check` clean. Pass 3 (LIVE label, year-axis tee/arrows/extension) verified via zoomed crops (PowerShell System.Drawing crop+brighten) desktop + emulated mobile.
- SCREENSHOT GOTCHA: chrome `--headless=new --window-size=414,...` does NOT emulate mobile — layout renders wider and gets clipped (looks like fake overflow). Use CDP `Emulation.setDeviceMetricsOverride {mobile:true}` for real mobile checks. Verified no actual horizontal overflow at 414px (docScrollW == 414).

## Simulated now (dev)
- `src/lib/now.ts`: `SIM_NOW` const + `getNow()`. Chart uses `getNow()` everywhere instead of `new Date()`.
- **`SIM_NOW = null` as of 2026-07-17** (real clock) — live bar now uses real ongoing data, no sim needed. Re-set to a mid-tournament ISO string only if reproducing a specific live moment.

## Known limitations / open questions for boss man
- ~~Live bar anniversary-based~~ RESOLVED 2026-07-17: now flag-based (`live`), decoupled from defending. Live event and defended event are matched independently (defending = same name, prior year; may be absent). See top section.
- ~~Placeholder live points (800/SF)~~ RESOLVED 2026-07-17: real round+points from ongoing draw.
- Data staleness: freshly loaded (includes Wimbledon 2026 + this week's ongoing 250s). Next `refresh:data` (~10-14 calls) picks up deeper rounds of ongoing events.
- ~~Empty live pane styling~~ RESOLVED: boss man decided blank-as-is (blank = not playing), keep alignment. (2026-07-13)
- ~~Red dot overlap~~ RESOLVED: dot + "LIVE" text moved above the pane's bar area per boss man. (2026-07-13)
